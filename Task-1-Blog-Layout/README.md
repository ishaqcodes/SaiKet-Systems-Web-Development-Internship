# Task 1: Basic Blog Post Layout

### 📝 Description
This task involved creating a structured blog post using **Semantic HTML5** tags.

### ✨ Features
- Used `<header>`, `<main>`, `<article>`, and `<footer>` tags.
- Included images and unordered lists.
- Clean and readable structure without CSS.
